'use strict';

/**
 * @param {UpdateCDNURLsOptions} optionDefinitions
 * @returns {void}
 */
function updateCDNURLs ({
  file, outputPath
}) {
  file.forEach((f) => {
    //
  });
}

exports.updateCDNURLs = updateCDNURLs;
